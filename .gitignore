# Rust
/target/
**/target/
*.rs.bk
*.pdb

# Python test/runtime caches
__pycache__/
*.py[cod]
.pytest_cache/

# Windows
Thumbs.db
Desktop.ini
$RECYCLE.BIN/
*.lnk

# Editors
.vs/
.vscode/
*.suo
*.user

# Note: vendor/ is intentionally NOT ignored — vendored libghostty-vt
# artifacts are checked in (see xtask vendor-vt).

# Soak harness output (scripts/soak.ps1, Task 13) — local run artifacts only.
/soak-results/
scripts/vtebench.exe
